# Copie para .env.local (nunca commite chaves reais).
# O app ignora Gemini direto no upstream; só OpenRouter entra em auto-config de dev.

OPENROUTER_API_KEY=

# Opcional: reativar API Gemini (Google) só na sua máquina
# FLOWKIT_GEMINI_ENABLE=1
